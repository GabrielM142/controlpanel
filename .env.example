APP_MODE=demo
# In production use APP_MODE=live. Set a long random password; never commit it.
PANEL_PASSWORD=
DATABASE_URL=
# Worker only: read-only account on Odoo PostgreSQL replica, never browser or Vercel.
ODOO_REPLICA_URL=
